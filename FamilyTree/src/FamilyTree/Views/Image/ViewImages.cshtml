@model List<FamilyTree.Models.Image>

@if (Model.Count != 0)
{
    foreach (var i in Model)
    {
        <img src="~/IMG/Uploads/@i.URI" />
    }
}
else
{
    <span> no images found</span>
}